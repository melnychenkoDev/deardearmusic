export default function glitchFX() {
	// const cagFx = document.querySelectorAll('.cagFx');
	// cagFx.forEach((el, i) => {
	// 	let spanCont = cagFx[i].innerText;
	// 	cagFx[i].insertAdjacentHTML('afterbegin', `<span class="glitch">${spanCont}</span><span class="glitch">${spanCont}</span>`);
	// });
}